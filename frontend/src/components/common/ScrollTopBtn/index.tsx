import React from 'react';
import styled from 'styled-components';
import { Box } from '../';

const WidgetWpapper = styled(Box)`
    
`;

export const ScrollTopBtn = () => {
    return (
        <Box>
            asdfsfd
        </Box>
    );
}